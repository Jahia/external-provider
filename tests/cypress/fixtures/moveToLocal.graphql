mutation moveToLocal($pathOrId: String! = "/mounts/mount-test", $localPath: String! = "/sites/digitall/files") {
    admin {
        mountPoint {
            modifyVfs(pathOrId: $pathOrId, mountPointRefPath: $localPath)
        }
    }
}
