query mountInfo {
    admin {
        mountPoint {
            mountPoints {
                uuid
                mountName
                mountStatus
                mountPointRefPath
                properties {key value}
            }
        }
    }
}

